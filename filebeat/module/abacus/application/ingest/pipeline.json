{
  "description": "Pipeline for parsing abacus application logs",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "parsedMessage"
      }
    }
  ],
  "on_failure" : [{
    "set" : {
      "field" : "error.message",
      "value" : "{{ _ingest.on_failure_message }}"
    }
  }]
}
